package com.bisket.api.repository;

import com.bisket.api.entity.AmusementBar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AmusementBarRepository extends JpaRepository<AmusementBar, Long> {
}
