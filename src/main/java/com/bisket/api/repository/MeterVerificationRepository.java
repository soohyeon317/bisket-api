package com.bisket.api.repository;

import com.bisket.api.entity.MeterVerification;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MeterVerificationRepository extends JpaRepository<MeterVerification, Long> {
}
