package com.bisket.api.repository;

import com.bisket.api.entity.HighPressureGas;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HighPressureGasRepository extends JpaRepository<HighPressureGas, Long> {
}
