package com.bisket.api.repository;

import com.bisket.api.entity.MusicRecordProduction;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MusicRecordProductionRepository extends JpaRepository<MusicRecordProduction, Long> {
}
