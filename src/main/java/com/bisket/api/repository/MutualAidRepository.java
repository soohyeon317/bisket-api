package com.bisket.api.repository;

import com.bisket.api.entity.MutualAid;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MutualAidRepository extends JpaRepository<MutualAid, Long> {
}
