package com.bisket.api.repository;

import com.bisket.api.entity.MusicRecordVideoDistribution;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MusicRecordVideoDistributionRepository extends JpaRepository<MusicRecordVideoDistribution, Long> {
}
