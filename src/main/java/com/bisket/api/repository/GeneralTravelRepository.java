package com.bisket.api.repository;

import com.bisket.api.entity.GeneralTravel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeneralTravelRepository extends JpaRepository<GeneralTravel, Long> {
}
