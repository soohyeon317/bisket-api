package com.bisket.api.repository;

import com.bisket.api.entity.Glasses;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GlassesRepository extends JpaRepository<Glasses, Long> {
}
