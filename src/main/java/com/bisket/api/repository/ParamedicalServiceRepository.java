package com.bisket.api.repository;

import com.bisket.api.entity.ParamedicalService;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParamedicalServiceRepository extends JpaRepository<ParamedicalService, Long> {
}
