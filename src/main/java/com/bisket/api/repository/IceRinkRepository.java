package com.bisket.api.repository;

import com.bisket.api.entity.IceRink;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IceRinkRepository extends JpaRepository<IceRink, Long> {
}
