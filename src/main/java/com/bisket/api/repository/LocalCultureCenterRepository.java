package com.bisket.api.repository;

import com.bisket.api.entity.LocalCultureCenter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LocalCultureCenterRepository extends JpaRepository<LocalCultureCenter, Long> {
}
