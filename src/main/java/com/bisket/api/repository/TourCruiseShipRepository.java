package com.bisket.api.repository;

import com.bisket.api.entity.TourCruiseShip;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TourCruiseShipRepository extends JpaRepository<TourCruiseShip, Long> {
}
