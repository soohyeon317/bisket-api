package com.bisket.api.repository;

import com.bisket.api.entity.HanokExperience;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HanokExperienceRepository extends JpaRepository<HanokExperience, Long> {
}
