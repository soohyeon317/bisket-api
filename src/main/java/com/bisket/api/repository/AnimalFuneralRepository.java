package com.bisket.api.repository;

import com.bisket.api.entity.AnimalFuneral;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AnimalFuneralRepository extends JpaRepository<AnimalFuneral, Long> {
}
