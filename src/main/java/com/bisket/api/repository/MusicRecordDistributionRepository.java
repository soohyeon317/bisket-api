package com.bisket.api.repository;

import com.bisket.api.entity.MusicRecordDistribution;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MusicRecordDistributionRepository extends JpaRepository<MusicRecordDistribution, Long> {
}
