package com.bisket.api.repository;

import com.bisket.api.entity.CompositeVideoService;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CompositeVideoServiceRepository extends JpaRepository<CompositeVideoService, Long> {
}
