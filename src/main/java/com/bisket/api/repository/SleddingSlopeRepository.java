package com.bisket.api.repository;

import com.bisket.api.entity.SleddingSlope;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SleddingSlopeRepository extends JpaRepository<SleddingSlope, Long> {
}
