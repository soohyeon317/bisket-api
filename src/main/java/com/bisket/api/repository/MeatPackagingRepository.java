package com.bisket.api.repository;

import com.bisket.api.entity.MeatPackaging;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MeatPackagingRepository extends JpaRepository<MeatPackaging, Long> {
}
