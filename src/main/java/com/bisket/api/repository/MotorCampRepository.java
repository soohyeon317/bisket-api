package com.bisket.api.repository;

import com.bisket.api.entity.MotorCamp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MotorCampRepository extends JpaRepository<MotorCamp, Long> {
}
