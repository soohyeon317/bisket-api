package com.bisket.api.repository;

import com.bisket.api.entity.MartialArtGym;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MartialArtGymRepository extends JpaRepository<MartialArtGym, Long> {
}
