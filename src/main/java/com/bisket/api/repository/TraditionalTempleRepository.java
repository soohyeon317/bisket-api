package com.bisket.api.repository;

import com.bisket.api.entity.TraditionalTemple;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TraditionalTempleRepository extends JpaRepository<TraditionalTemple, Long> {
}
